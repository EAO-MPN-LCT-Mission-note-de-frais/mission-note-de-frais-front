/**
 * Represents a transport mode used during a mission.
 */
export interface Transport {
  /**
   * The unique identifier for the transport.
   */
  id: number;

  /**
   * The name of the transport mode.
   */
  name: string;
}
